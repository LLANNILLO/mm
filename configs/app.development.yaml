logging:
  level: debug
  seq:
    endpoint: "http://localhost:5341"

cache:
  address: "evently.valkey:6379"

authentication:
  issuer_url: "http://evently.identity:8080/realms/evently"
  audience: "account"
